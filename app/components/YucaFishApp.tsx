"use client";
/* eslint-disable @next/next/no-img-element -- protected R2 images use authenticated API URLs */

import { FormEvent, useEffect, useMemo, useState } from "react";
import {
  Anchor, Award, BarChart3, Bell, CalendarDays, Camera, ChevronDown, ChevronLeft,
  CircleHelp, Compass, Edit3, Fish, Gauge, History, Home, Image as ImageIcon,
  LogOut, MapPin, Menu, MoreHorizontal, Plus, Search, ShieldCheck, Ship,
  SlidersHorizontal, Trash2, Trophy, UserRound, Waves, Weight, X,
} from "lucide-react";

type Profile = { email: string; displayName: string; firstName: string; lastName: string; city: string; state: string; country: string; timezone: string; weightUnit: "kg" | "lb"; role: "USER" | "ADMIN"; status: string };
type Trip = { id: string; title: string; port: string; fishingDate: string; departureTime?: string; returnTime?: string; area?: string; vessel?: string; captain?: string; notes?: string; status: "DRAFT" | "COMPLETED"; createdAt: string };
type Catch = { id: string; tripId: string; species: string; customSpecies?: boolean | number; weightKg: number; originalWeight: number; originalUnit: string; releaseStatus: "RELEASED" | "KEPT" | "UNSPECIFIED"; lengthCm?: number; caughtAt?: string; lure?: string; notes?: string };
type CatalogItem = { id: string; commonName?: string; aliases?: string; name?: string; type?: string; municipality?: string; active: number | boolean };
type Media = { id: string; tripId: string; catchId: string; url: string; altText?: string };
type AppData = { profile: Profile; trips: Trip[]; catches: Catch[]; species: CatalogItem[]; ports: CatalogItem[]; media: Media[]; logs: Array<Record<string, string>> };
type View = "dashboard" | "history" | "stats" | "profile" | "admin" | "trip";

const emptyData: AppData = { profile: { email: "", displayName: "", firstName: "", lastName: "", city: "", state: "Yucatán", country: "México", timezone: "America/Merida", weightUnit: "kg", role: "USER", status: "ACTIVE" }, trips: [], catches: [], species: [], ports: [], media: [], logs: [] };
const mxDate = () => new Intl.DateTimeFormat("en-CA", { timeZone: "America/Merida", year: "numeric", month: "2-digit", day: "2-digit" }).format(new Date());
const formatDate = (date: string, long = false) => new Intl.DateTimeFormat("es-MX", long ? { weekday: "long", day: "numeric", month: "long", year: "numeric", timeZone: "UTC" } : { day: "numeric", month: "short", year: "numeric", timeZone: "UTC" }).format(new Date(`${date}T12:00:00Z`));
const kgTo = (kg: number, unit: string) => unit === "lb" ? kg * 2.20462262 : kg;
const weightLabel = (kg: number, unit: string) => `${kgTo(kg, unit).toLocaleString("es-MX", { maximumFractionDigits: 2 })} ${unit}`;

export default function YucaFishApp() {
  const [data, setData] = useState<AppData>(emptyData);
  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");
  const [toast, setToast] = useState("");
  const [view, setView] = useState<View>("dashboard");
  const [menuOpen, setMenuOpen] = useState(false);
  const [tripForm, setTripForm] = useState<Trip | null>(null);
  const [selectedTrip, setSelectedTrip] = useState<string | null>(null);
  const [catchForm, setCatchForm] = useState<{ tripId: string; item?: Catch } | null>(null);
  const [confirm, setConfirm] = useState<{ title: string; body: string; action: () => void } | null>(null);
  const [period, setPeriod] = useState("all");

  useEffect(() => { void load(); }, []);
  async function load() {
    setLoading(true); setError("");
    try {
      const res = await fetch("/api/yucafish", { cache: "no-store" });
      const body = await res.json() as AppData & { error?: string };
      if (!res.ok) throw new Error(body.error || "No pudimos cargar tu bitácora.");
      setData(body);
    } catch (e) { setError(e instanceof Error ? e.message : "No pudimos cargar tu bitácora."); }
    finally { setLoading(false); }
  }
  async function mutate(payload: Record<string, unknown>, success: string) {
    setSaving(true); setError("");
    try {
      const res = await fetch("/api/yucafish", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) });
      const body = await res.json() as { data?: AppData; error?: string; id?: string };
      if (!res.ok) throw new Error(body.error || "No pudimos guardar los cambios.");
      if (body.data) setData(body.data);
      setToast(success); window.setTimeout(() => setToast(""), 3200);
      return body;
    } catch (e) { setError(e instanceof Error ? e.message : "No pudimos guardar los cambios."); throw e; }
    finally { setSaving(false); }
  }
  function navigate(next: View) { setView(next); setMenuOpen(false); setSelectedTrip(null); window.scrollTo({ top: 0, behavior: "smooth" }); }
  function openTrip(id: string) { setSelectedTrip(id); setView("trip"); setMenuOpen(false); window.scrollTo({ top: 0, behavior: "smooth" }); }

  if (loading) return <LoadingScreen />;
  if (error && !data.profile.email) return <ErrorScreen message={error} retry={load} />;

  return (
    <div className="app-shell">
      <Sidebar view={view} navigate={navigate} open={menuOpen} close={() => setMenuOpen(false)} role={data.profile.role} />
      <div className="app-main">
        <Topbar profile={data.profile} openMenu={() => setMenuOpen(true)} navigate={navigate} />
        <main className="content" id="main-content">
          {error && <div className="alert error" role="alert"><CircleHelp size={18} />{error}<button onClick={() => setError("")} aria-label="Cerrar mensaje"><X size={16} /></button></div>}
          {view === "dashboard" && <Dashboard data={data} period={period} setPeriod={setPeriod} newTrip={() => setTripForm(newTrip())} openTrip={openTrip} />}
          {view === "history" && <HistoryView data={data} newTrip={() => setTripForm(newTrip())} openTrip={openTrip} editTrip={setTripForm} />}
          {view === "stats" && <StatsView data={data} period={period} setPeriod={setPeriod} />}
          {view === "profile" && <ProfileView data={data} save={async (payload) => { await mutate({ op: "updateProfile", ...payload }, "Perfil actualizado"); }} saving={saving} />}
          {view === "admin" && <AdminView data={data} mutate={mutate} saving={saving} />}
          {view === "trip" && selectedTrip && <TripDetail data={data} tripId={selectedTrip} back={() => navigate("history")} addCatch={() => setCatchForm({ tripId: selectedTrip })} editCatch={(item) => setCatchForm({ tripId: selectedTrip, item })} editTrip={(trip) => setTripForm(trip)} duplicate={() => void mutate({ op: "duplicateTrip", id: selectedTrip }, "Pesca duplicada como borrador").then(() => navigate("history"))} deleteTrip={() => setConfirm({ title: "¿Eliminar esta pesca?", body: "La pesca y sus capturas dejarán de aparecer en tu bitácora.", action: () => void mutate({ op: "deleteTrip", id: selectedTrip }, "Pesca eliminada").then(() => { setConfirm(null); navigate("history"); }) })} deleteCatch={(id) => setConfirm({ title: "¿Eliminar esta captura?", body: "Esta acción retirará la captura de las estadísticas.", action: () => void mutate({ op: "deleteCatch", id }, "Captura eliminada").then(() => setConfirm(null)) })} />}
        </main>
      </div>
      <MobileNav view={view} navigate={navigate} newTrip={() => setTripForm(newTrip())} />
      {tripForm && <TripForm item={tripForm} ports={data.ports} saving={saving} close={() => setTripForm(null)} save={async (payload, addFish) => { const op = tripForm.id ? "updateTrip" : "createTrip"; await mutate({ op, id: tripForm.id, ...payload }, tripForm.id ? "Pesca actualizada" : "Pesca guardada"); setTripForm(null); if (addFish) { const newest = data.trips[0]; navigate("history"); if (newest) openTrip(newest.id); } else navigate("history"); }} />}
      {catchForm && <CatchForm config={catchForm} species={data.species} saving={saving} close={() => setCatchForm(null)} save={async (payload, file) => { const op = catchForm.item ? "updateCatch" : "createCatch"; const result = await mutate({ op, id: catchForm.item?.id, tripId: catchForm.tripId, ...payload }, catchForm.item ? "Captura actualizada" : "¡Captura registrada!"); const catchId = catchForm.item?.id || result?.id; if (file && catchId) { const form = new FormData(); form.set("file", file); form.set("tripId", catchForm.tripId); form.set("catchId", catchId); const upload = await fetch("/api/media", { method: "POST", body: form }); const uploadBody = await upload.json() as { error?: string }; if (!upload.ok) setError(uploadBody.error || "La captura se guardó, pero no la fotografía."); else await load(); } setCatchForm(null); }} />}
      {confirm && <ConfirmDialog {...confirm} close={() => setConfirm(null)} saving={saving} />}
      {toast && <div className="toast" role="status"><ShieldCheck size={18} />{toast}</div>}
    </div>
  );
}

function newTrip(): Trip { return { id: "", title: "", port: "", fishingDate: mxDate(), status: "DRAFT", createdAt: "" }; }

function Brand({ compact = false }: { compact?: boolean }) { return <div className={`brand ${compact ? "compact" : ""}`}><span className="brand-mark"><Fish size={25} /></span>{!compact && <span><strong>YucaFish</strong><small>Bitácora de pesca</small></span>}</div>; }

function Sidebar({ view, navigate, open, close, role }: { view: View; navigate: (v: View) => void; open: boolean; close: () => void; role: string }) {
  const items: Array<[View, string, typeof Home]> = [["dashboard", "Inicio", Home], ["history", "Mis pescas", History], ["stats", "Estadísticas", BarChart3], ["profile", "Mi perfil", UserRound]];
  return <><aside className={`sidebar ${open ? "open" : ""}`} aria-label="Navegación principal"><div className="sidebar-head"><Brand /><button className="icon-button mobile-only" onClick={close} aria-label="Cerrar menú"><X /></button></div><div className="nav-group"><p>BITÁCORA</p>{items.map(([id, label, Icon]) => <button key={id} className={view === id || (view === "trip" && id === "history") ? "active" : ""} onClick={() => navigate(id)}><Icon size={19} />{label}</button>)}</div>{role === "ADMIN" && <div className="nav-group"><p>GESTIÓN</p><button className={view === "admin" ? "active" : ""} onClick={() => navigate("admin")}><ShieldCheck size={19} />Administración</button></div>}<div className="responsible-card"><span><Waves size={22} /></span><strong>Pesca responsable</strong><p>Consulta siempre las regulaciones oficiales antes de salir.</p></div><div className="sidebar-footer"><a href="/signout-with-chatgpt?return_to=/"><LogOut size={18} />Cerrar sesión</a></div></aside>{open && <button className="scrim mobile-only" onClick={close} aria-label="Cerrar menú" />}</>;
}

function Topbar({ profile, openMenu, navigate }: { profile: Profile; openMenu: () => void; navigate: (v: View) => void }) { return <header className="topbar"><button className="icon-button mobile-only" onClick={openMenu} aria-label="Abrir menú"><Menu /></button><div className="top-search"><Search size={18} /><input aria-label="Buscar en YucaFish" placeholder="Buscar pescas, puertos o especies" onFocus={() => navigate("history")} readOnly /></div><button className="icon-button" aria-label="Notificaciones"><Bell size={19} /><i /></button><button className="user-chip" onClick={() => navigate("profile")}><span>{initials(profile.displayName)}</span><b>{profile.displayName || "Pescador"}<small>{profile.role === "ADMIN" ? "Administrador" : "Mi cuenta"}</small></b><ChevronDown size={16} /></button></header>; }

function MobileNav({ view, navigate, newTrip }: { view: View; navigate: (v: View) => void; newTrip: () => void }) { return <nav className="mobile-nav" aria-label="Navegación móvil"><button className={view === "dashboard" ? "active" : ""} onClick={() => navigate("dashboard")}><Home /><span>Inicio</span></button><button className={view === "history" || view === "trip" ? "active" : ""} onClick={() => navigate("history")}><History /><span>Historial</span></button><button className="new-fab" onClick={newTrip} aria-label="Nueva pesca"><Plus /></button><button className={view === "stats" ? "active" : ""} onClick={() => navigate("stats")}><BarChart3 /><span>Estadísticas</span></button><button className={view === "profile" ? "active" : ""} onClick={() => navigate("profile")}><UserRound /><span>Perfil</span></button></nav>; }

function PageHeader({ eyebrow, title, subtitle, action }: { eyebrow?: string; title: string; subtitle?: string; action?: React.ReactNode }) { return <div className="page-header"><div>{eyebrow && <span className="eyebrow">{eyebrow}</span>}<h1>{title}</h1>{subtitle && <p>{subtitle}</p>}</div>{action}</div>; }
function PrimaryButton({ children, onClick, type = "button", disabled = false }: { children: React.ReactNode; onClick?: () => void; type?: "button" | "submit"; disabled?: boolean }) { return <button className="button primary" type={type} onClick={onClick} disabled={disabled}>{children}</button>; }
function PeriodTabs({ value, setValue }: { value: string; setValue: (v: string) => void }) { return <div className="period-tabs" role="group" aria-label="Periodo">{[["week", "Semana"], ["month", "Mes"], ["year", "Año"], ["all", "Todo"]].map(([id, label]) => <button key={id} className={value === id ? "active" : ""} onClick={() => setValue(id)}>{label}</button>)}</div>; }

function Dashboard({ data, period, setPeriod, newTrip, openTrip }: { data: AppData; period: string; setPeriod: (v: string) => void; newTrip: () => void; openTrip: (id: string) => void }) {
  const stats = calculate(data, period);
  return <><PageHeader eyebrow={formatDate(mxDate(), true)} title={`¡Buena pesca, ${data.profile.firstName || data.profile.displayName.split(" ")[0]}!`} subtitle="Aquí tienes el resumen de tu bitácora." action={<PrimaryButton onClick={newTrip}><Plus size={18} />Nueva pesca</PrimaryButton>} />
    <PeriodTabs value={period} setValue={setPeriod} />
    {data.trips.length === 0 ? <EmptyState newTrip={newTrip} /> : <>
      <div className="stat-grid"><Stat icon={Ship} label="Pescas" value={stats.trips.length.toString()} trend="+1 este mes" /><Stat icon={Fish} label="Peces" value={stats.catches.length.toString()} trend={`${stats.released} liberados`} /><Stat icon={Weight} label="Peso total" value={weightLabel(stats.totalWeight, data.profile.weightUnit)} trend="Acumulado" /><Stat icon={Trophy} label="Récord personal" value={weightLabel(stats.heaviest, data.profile.weightUnit)} trend={stats.topSpecies || "Sin especie"} /></div>
      <div className="dashboard-grid"><div className="card chart-card"><CardTitle title="Actividad de pesca" subtitle="Capturas registradas por mes" /><ActivityChart data={data} /><p className="chart-summary">Tu mes con mayor actividad registra {Math.max(...monthCounts(data).map((x) => x.value), 0)} capturas.</p></div><div className="card species-card"><CardTitle title="Especies favoritas" subtitle="Distribución de tus capturas" /><SpeciesDonut catches={stats.catches} /></div></div>
      <div className="card recent-card"><CardTitle title="Actividad reciente" subtitle="Tus últimas salidas" action={<button className="link-button" onClick={() => openTrip(data.trips[0]?.id)}>Ver detalle</button>} /><div className="trip-list">{data.trips.slice(0, 4).map((trip) => <TripRow key={trip.id} trip={trip} catches={data.catches} unit={data.profile.weightUnit} open={() => openTrip(trip.id)} />)}</div></div>
      <div className="achievements"><CardTitle title="Logros personales" subtitle="Pequeños hitos de tu aventura" /><div className="achievement-row"><Achievement icon={Anchor} name="Primera pesca" earned={data.trips.length >= 1} /><Achievement icon={Camera} name="Primer recuerdo" earned={data.media.length >= 1} /><Achievement icon={Fish} name="5 especies" earned={new Set(data.catches.map((c) => c.species)).size >= 5} /><Achievement icon={Award} name="10 pescas" earned={data.trips.length >= 10} /></div></div>
    </>}
  </>;
}

function Stat({ icon: Icon, label, value, trend }: { icon: typeof Fish; label: string; value: string; trend: string }) { return <div className="card stat-card"><span className="stat-icon"><Icon /></span><div><p>{label}</p><strong>{value}</strong><small>{trend}</small></div></div>; }
function CardTitle({ title, subtitle, action }: { title: string; subtitle?: string; action?: React.ReactNode }) { return <div className="card-title"><div><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div>{action}</div>; }
function ActivityChart({ data }: { data: AppData }) { const months = monthCounts(data); const max = Math.max(...months.map((m) => m.value), 1); return <div className="bar-chart" aria-label="Gráfica de capturas por mes">{months.map((m) => <div key={m.label} className="bar-column"><span>{m.value || ""}</span><i style={{ height: `${Math.max((m.value / max) * 100, 4)}%` }} /><small>{m.label}</small></div>)}</div>; }
function SpeciesDonut({ catches }: { catches: Catch[] }) { const grouped = groupSpecies(catches).slice(0, 4); const total = catches.length || 1; const colors = ["#1479f8", "#21b6a8", "#f59e0b", "#8b5cf6"]; const gradient = grouped.reduce<{ stops: string[]; end: number }>((acc, g, i) => { const end = acc.end + (g.count / total) * 100; return { stops: [...acc.stops, `${colors[i]} ${acc.end}% ${end}%`], end }; }, { stops: [], end: 0 }); const stops = gradient.end < 100 ? [...gradient.stops, `#edf2f7 ${gradient.end}% 100%`] : gradient.stops; return <div className="donut-wrap"><div className="donut" style={{ background: `conic-gradient(${stops.join(",")})` }}><span><b>{catches.length}</b>capturas</span></div><div className="legend">{grouped.map((g, i) => <div key={g.name}><i style={{ background: colors[i] }} /><span>{g.name}</span><b>{Math.round(g.count / total * 100)}%</b></div>)}</div></div>; }
function Achievement({ icon: Icon, name, earned }: { icon: typeof Fish; name: string; earned: boolean }) { return <div className={`achievement ${earned ? "earned" : ""}`}><span><Icon /></span><div><strong>{name}</strong><small>{earned ? "¡Conseguido!" : "Sigue pescando"}</small></div></div>; }

function HistoryView({ data, newTrip, openTrip, editTrip }: { data: AppData; newTrip: () => void; openTrip: (id: string) => void; editTrip: (t: Trip) => void }) {
  const [query, setQuery] = useState(""); const [status, setStatus] = useState("ALL"); const [sort, setSort] = useState("recent");
  const filtered = useMemo(() => [...data.trips].filter((t) => `${t.title} ${t.port}`.toLowerCase().includes(query.toLowerCase())).filter((t) => status === "ALL" || t.status === status).sort((a, b) => sort === "weight" ? tripWeight(b.id, data.catches) - tripWeight(a.id, data.catches) : sort === "fish" ? tripCatches(b.id, data.catches).length - tripCatches(a.id, data.catches).length : b.fishingDate.localeCompare(a.fishingDate)), [data, query, status, sort]);
  return <><PageHeader eyebrow="Tu archivo personal" title="Mis pescas" subtitle={`${data.trips.length} salidas guardadas en tu bitácora`} action={<PrimaryButton onClick={newTrip}><Plus size={18} />Nueva pesca</PrimaryButton>} /><div className="filters card"><label className="search-field"><Search size={18} /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Buscar por título o puerto" /></label><select value={status} onChange={(e) => setStatus(e.target.value)} aria-label="Filtrar por estado"><option value="ALL">Todos los estados</option><option value="COMPLETED">Finalizadas</option><option value="DRAFT">Borradores</option></select><select value={sort} onChange={(e) => setSort(e.target.value)} aria-label="Ordenar pescas"><option value="recent">Más recientes</option><option value="fish">Mayor cantidad</option><option value="weight">Mayor peso</option></select><button className="icon-button"><SlidersHorizontal size={18} /></button></div>{filtered.length ? <div className="trip-card-grid">{filtered.map((trip) => <TripCard key={trip.id} trip={trip} catches={data.catches} media={data.media} unit={data.profile.weightUnit} open={() => openTrip(trip.id)} edit={() => editTrip(trip)} />)}</div> : <div className="card no-results"><Search /><h2>No encontramos pescas</h2><p>Prueba otra búsqueda o registra una salida nueva.</p><PrimaryButton onClick={newTrip}><Plus size={18} />Nueva pesca</PrimaryButton></div>}</>;
}

function TripCard({ trip, catches, media, unit, open, edit }: { trip: Trip; catches: Catch[]; media: Media[]; unit: string; open: () => void; edit: () => void }) { const list = tripCatches(trip.id, catches); const photo = media.find((m) => m.tripId === trip.id); return <article className="card trip-card"><button className="trip-cover" onClick={open} aria-label={`Abrir ${trip.title}`}>{photo ? <img src={photo.url} alt={photo.altText || trip.title} /> : <span><Waves /><Fish /></span>}<em className={trip.status === "COMPLETED" ? "complete" : "draft"}>{trip.status === "COMPLETED" ? "Finalizada" : "Borrador"}</em></button><div className="trip-card-body"><div><p className="trip-date"><CalendarDays size={15} />{formatDate(trip.fishingDate)}</p><h2><button onClick={open}>{trip.title}</button></h2><p><MapPin size={15} />{trip.port}</p></div><div className="trip-metrics"><span><Fish size={17} /><b>{list.length}</b> peces</span><span><Weight size={17} /><b>{weightLabel(tripWeight(trip.id, catches), unit)}</b> total</span></div><div className="trip-actions"><button className="button secondary" onClick={open}>Ver detalle</button><button className="icon-button" onClick={edit} aria-label="Editar pesca"><Edit3 size={18} /></button></div></div></article>; }
function TripRow({ trip, catches, unit, open }: { trip: Trip; catches: Catch[]; unit: string; open: () => void }) { const list = tripCatches(trip.id, catches); return <button className="trip-row" onClick={open}><span className="row-icon"><Ship /></span><span className="row-main"><b>{trip.title}</b><small><MapPin size={13} />{trip.port} · {formatDate(trip.fishingDate)}</small></span><span><b>{list.length}</b><small>peces</small></span><span><b>{weightLabel(tripWeight(trip.id, catches), unit)}</b><small>peso total</small></span><ChevronDown size={18} /></button>; }

function TripDetail({ data, tripId, back, addCatch, editCatch, editTrip, duplicate, deleteTrip, deleteCatch }: { data: AppData; tripId: string; back: () => void; addCatch: () => void; editCatch: (c: Catch) => void; editTrip: (t: Trip) => void; duplicate: () => void; deleteTrip: () => void; deleteCatch: (id: string) => void }) {
  const trip = data.trips.find((t) => t.id === tripId); if (!trip) return <ErrorScreen message="No encontramos esta pesca." retry={back} />; const list = tripCatches(tripId, data.catches); const heaviest = Math.max(...list.map((c) => c.weightKg), 0);
  return <><button className="back-link" onClick={back}><ChevronLeft size={18} />Volver a mis pescas</button><div className="trip-hero"><div><span className={`status ${trip.status.toLowerCase()}`}>{trip.status === "COMPLETED" ? "Finalizada" : "Borrador"}</span><h1>{trip.title}</h1><p><CalendarDays size={17} />{formatDate(trip.fishingDate, true)} <i /> <MapPin size={17} />{trip.port}</p></div><div className="header-actions"><button className="button secondary" onClick={() => editTrip(trip)}><Edit3 size={17} />Editar</button><button className="icon-button" onClick={duplicate} aria-label="Duplicar pesca"><MoreHorizontal /></button><button className="icon-button danger" onClick={deleteTrip} aria-label="Eliminar pesca"><Trash2 /></button></div></div><div className="detail-stats"><Stat icon={Fish} label="Capturas" value={String(list.length)} trend={`${new Set(list.map((c) => c.species)).size} especies`} /><Stat icon={Weight} label="Peso total" value={weightLabel(tripWeight(tripId, data.catches), data.profile.weightUnit)} trend="En esta salida" /><Stat icon={Trophy} label="Más pesada" value={weightLabel(heaviest, data.profile.weightUnit)} trend={list.sort((a, b) => b.weightKg - a.weightKg)[0]?.species || "Sin capturas"} /></div><div className="detail-grid"><section className="card"><CardTitle title="Capturas" subtitle="Peces registrados en esta salida" action={<PrimaryButton onClick={addCatch}><Plus size={17} />Agregar pez</PrimaryButton>} />{list.length ? <div className="catch-list">{list.map((item) => <CatchRow key={item.id} item={item} media={data.media} unit={data.profile.weightUnit} edit={() => editCatch(item)} remove={() => deleteCatch(item.id)} />)}</div> : <div className="empty-inline"><span><Fish /></span><h3>Aún no hay peces</h3><p>Registra tu primera captura de esta salida.</p><PrimaryButton onClick={addCatch}><Plus size={17} />Agregar pez</PrimaryButton></div>}</section><aside className="card trip-info"><CardTitle title="Detalles de la salida" /><Info icon={Ship} label="Embarcación" value={trip.vessel || "Sin especificar"} /><Info icon={Gauge} label="Capitán" value={trip.captain || "Sin especificar"} /><Info icon={Compass} label="Zona" value={trip.area || "Sin especificar"} /><Info icon={CalendarDays} label="Horario" value={trip.departureTime ? `${trip.departureTime} – ${trip.returnTime || "—"}` : "Sin especificar"} />{trip.notes && <div className="notes"><b>Notas</b><p>{trip.notes}</p></div>}</aside></div></>;
}
function CatchRow({ item, media, unit, edit, remove }: { item: Catch; media: Media[]; unit: string; edit: () => void; remove: () => void }) { const photo = media.find((m) => m.catchId === item.id); return <div className="catch-row"><div className="catch-photo">{photo ? <img src={photo.url} alt={photo.altText || item.species} /> : <Fish />}</div><div className="catch-main"><h3>{item.species}</h3><p>{item.lure || "Sin señuelo registrado"}</p><span className={`release ${item.releaseStatus.toLowerCase()}`}>{item.releaseStatus === "RELEASED" ? "Liberado" : item.releaseStatus === "KEPT" ? "Conservado" : "Sin especificar"}</span></div><div className="catch-weight"><strong>{weightLabel(item.weightKg, unit)}</strong><small>{item.lengthCm ? `${item.lengthCm} cm` : "Peso individual"}</small></div><button className="icon-button" onClick={edit} aria-label={`Editar ${item.species}`}><Edit3 size={17} /></button><button className="icon-button danger" onClick={remove} aria-label={`Eliminar ${item.species}`}><Trash2 size={17} /></button></div>; }
function Info({ icon: Icon, label, value }: { icon: typeof Fish; label: string; value: string }) { return <div className="info-row"><span><Icon /></span><div><small>{label}</small><b>{value}</b></div></div>; }

function StatsView({ data, period, setPeriod }: { data: AppData; period: string; setPeriod: (v: string) => void }) { const s = calculate(data, period); const ports = Object.entries(s.trips.reduce<Record<string, number>>((a, t) => ({ ...a, [t.port]: (a[t.port] || 0) + 1 }), {})).sort((a, b) => b[1] - a[1]); return <><PageHeader eyebrow="Tus números" title="Estadísticas" subtitle="Descubre patrones y celebra tus mejores capturas." /><PeriodTabs value={period} setValue={setPeriod} /><div className="stat-grid five"><Stat icon={Ship} label="Total pescas" value={String(s.trips.length)} trend="Salidas" /><Stat icon={Fish} label="Capturas" value={String(s.catches.length)} trend={`${new Set(s.catches.map((c) => c.species)).size} especies`} /><Stat icon={Weight} label="Peso total" value={weightLabel(s.totalWeight, data.profile.weightUnit)} trend={`Promedio ${weightLabel(s.catches.length ? s.totalWeight / s.catches.length : 0, data.profile.weightUnit)}`} /><Stat icon={Trophy} label="Más pesada" value={weightLabel(s.heaviest, data.profile.weightUnit)} trend={s.topSpecies || "—"} /><Stat icon={Waves} label="Liberación" value={`${s.catches.length ? Math.round(s.released / s.catches.length * 100) : 0}%`} trend={`${s.released} peces`} /></div><div className="dashboard-grid"><div className="card chart-card"><CardTitle title="Peso por salida" subtitle="Kilogramos acumulados" /><div className="horizontal-bars">{s.trips.slice(0, 6).map((t) => <div key={t.id}><span>{t.title}</span><i><b style={{ width: `${Math.min(tripWeight(t.id, s.catches) / Math.max(...s.trips.map((x) => tripWeight(x.id, s.catches)), 1) * 100, 100)}%` }} /></i><strong>{weightLabel(tripWeight(t.id, s.catches), data.profile.weightUnit)}</strong></div>)}</div></div><div className="card"><CardTitle title="Puertos frecuentes" subtitle="Tus puntos de salida favoritos" /><div className="ranking">{ports.slice(0, 5).map(([name, count], i) => <div key={name}><span>{i + 1}</span><MapPin /><b>{name}</b><strong>{count} {count === 1 ? "salida" : "salidas"}</strong></div>)}</div></div></div><div className="card"><CardTitle title="Capturas por mes" subtitle="Resumen visual de actividad" /><ActivityChart data={{ ...data, trips: s.trips, catches: s.catches }} /></div></>;
}

function ProfileView({ data, save, saving }: { data: AppData; save: (p: Record<string, string>) => Promise<void>; saving: boolean }) { const [form, setForm] = useState(data.profile); const update = (key: keyof Profile, value: string) => setForm({ ...form, [key]: value }); return <><PageHeader eyebrow="Tu cuenta" title="Perfil y preferencias" subtitle="Personaliza cómo ves y registras tu bitácora." /><form className="profile-layout" onSubmit={(e) => { e.preventDefault(); void save(form as unknown as Record<string, string>); }}><div className="card profile-card"><div className="profile-avatar">{initials(form.displayName)}<button type="button" aria-label="Cambiar fotografía"><Camera size={16} /></button></div><h2>{form.displayName}</h2><p>{form.email}</p><span className="verified"><ShieldCheck size={15} />Cuenta verificada</span><hr /><p className="privacy-note"><ShieldCheck />Tus pescas y fotografías son privadas. Solo tú puedes acceder a ellas.</p></div><div className="card form-card"><CardTitle title="Información personal" /><div className="form-grid"><Field label="Nombre" value={form.firstName} set={(v) => update("firstName", v)} required /><Field label="Apellidos" value={form.lastName} set={(v) => update("lastName", v)} /><Field label="Nombre visible" value={form.displayName} set={(v) => update("displayName", v)} required wide /><Field label="Ciudad" value={form.city} set={(v) => update("city", v)} /><Field label="Estado" value={form.state} set={(v) => update("state", v)} /><Field label="País" value={form.country} set={(v) => update("country", v)} /><label>Zona horaria<select value={form.timezone} onChange={(e) => update("timezone", e.target.value)}><option>America/Merida</option><option>America/Mexico_City</option><option>America/Cancun</option></select></label><label>Unidad de peso<select value={form.weightUnit} onChange={(e) => update("weightUnit", e.target.value)}><option value="kg">Kilogramos (kg)</option><option value="lb">Libras (lb)</option></select></label></div><div className="form-footer"><PrimaryButton type="submit" disabled={saving}>{saving ? "Guardando…" : "Guardar cambios"}</PrimaryButton></div></div><div className="card account-card"><CardTitle title="Seguridad de la cuenta" /><div><span><ShieldCheck /></span><b>Acceso protegido</b><p>Tu identidad se verifica mediante inicio de sesión seguro. YucaFish nunca almacena tu contraseña.</p></div><a className="button secondary" href="/signout-with-chatgpt?return_to=/">Cerrar todas las sesiones</a></div></form></>;
}

function AdminView({ data, mutate, saving }: { data: AppData; mutate: (p: Record<string, unknown>, s: string) => Promise<unknown>; saving: boolean }) { const [tab, setTab] = useState<"overview" | "species" | "ports" | "audit">("overview"); const [name, setName] = useState(""); if (data.profile.role !== "ADMIN") return <ErrorScreen message="No tienes permiso para entrar al panel administrativo." retry={() => location.assign("/app")} />; return <><PageHeader eyebrow="Panel protegido" title="Administración" subtitle="Gestiona catálogos y revisa la salud general de YucaFish." /><div className="admin-tabs">{[["overview", "Resumen"], ["species", "Especies"], ["ports", "Puertos"], ["audit", "Auditoría"]].map(([id, label]) => <button key={id} className={tab === id ? "active" : ""} onClick={() => setTab(id as typeof tab)}>{label}</button>)}</div>{tab === "overview" && <><div className="stat-grid"><Stat icon={UserRound} label="Usuarios" value="1" trend="1 verificado" /><Stat icon={Ship} label="Pescas" value={String(data.trips.length)} trend="Registros activos" /><Stat icon={Fish} label="Capturas" value={String(data.catches.length)} trend="En la plataforma" /><Stat icon={ImageIcon} label="Fotografías" value={String(data.media.length)} trend="Almacenamiento privado" /></div><div className="card admin-welcome"><span><ShieldCheck /></span><div><h2>Todo en orden</h2><p>Los catálogos están activos y las operaciones sensibles quedan registradas sin exponer datos privados.</p></div></div></>}{(tab === "species" || tab === "ports") && <div className="card admin-table"><CardTitle title={tab === "species" ? "Catálogo de especies" : "Marinas y puertos"} subtitle="Activa, desactiva o agrega opciones al selector." action={<form className="inline-add" onSubmit={(e) => { e.preventDefault(); if (!name.trim()) return; void mutate({ op: tab === "species" ? "createSpecies" : "createPort", name }, tab === "species" ? "Especie agregada" : "Puerto agregado").then(() => setName("")); }}><input value={name} onChange={(e) => setName(e.target.value)} placeholder={tab === "species" ? "Nueva especie" : "Nuevo puerto"} /><PrimaryButton type="submit" disabled={saving}><Plus size={16} />Agregar</PrimaryButton></form>} /><div className="table-wrap"><table><thead><tr><th>Nombre</th><th>Alias / tipo</th><th>Estado</th><th>Acción</th></tr></thead><tbody>{(tab === "species" ? data.species : data.ports).map((item) => <tr key={item.id}><td><Fish size={17} />{item.commonName || item.name}</td><td>{item.aliases || item.type || "—"}</td><td><span className={item.active ? "active-dot" : "inactive-dot"}>{item.active ? "Activo" : "Inactivo"}</span></td><td><button className="button secondary small" onClick={() => void mutate({ op: tab === "species" ? "toggleSpecies" : "togglePort", id: item.id }, "Estado actualizado")}>{item.active ? "Desactivar" : "Activar"}</button></td></tr>)}</tbody></table></div></div>}{tab === "audit" && <div className="card admin-table"><CardTitle title="Registro de auditoría" subtitle="Eventos técnicos recientes; los correos se almacenan como hash." /><div className="table-wrap"><table><thead><tr><th>Fecha</th><th>Acción</th><th>Entidad</th><th>Actor</th></tr></thead><tbody>{data.logs.map((log, i) => <tr key={String(log.id || i)}><td>{new Date(String(log.createdAt)).toLocaleString("es-MX")}</td><td>{String(log.action).replaceAll("_", " ")}</td><td>{String(log.entityType)}</td><td><code>{String(log.actorEmailHash)}</code></td></tr>)}</tbody></table></div></div>}</>;
}

function TripForm({ item, ports, save, close, saving }: { item: Trip; ports: CatalogItem[]; save: (p: Record<string, string>, addFish: boolean) => Promise<void>; close: () => void; saving: boolean }) { const [form, setForm] = useState({ title: item.title, port: item.port, fishingDate: item.fishingDate || mxDate(), departureTime: item.departureTime || "", returnTime: item.returnTime || "", area: item.area || "", vessel: item.vessel || "", captain: item.captain || "", notes: item.notes || "", status: item.status || "DRAFT" }); const [customPort, setCustomPort] = useState(""); const update = (key: string, value: string) => setForm({ ...form, [key]: value }); const submit = async (e: FormEvent, addFish = false) => { e.preventDefault(); if (!form.title.trim() || !(form.port === "Otro" ? customPort.trim() : form.port)) return; await save({ ...form, port: form.port === "Otro" ? customPort : form.port }, addFish); }; return <div className="modal-layer" role="dialog" aria-modal="true" aria-labelledby="trip-form-title"><div className="sheet wide-sheet"><div className="sheet-head"><div><span className="eyebrow">PASO 1 DE 2 · INFORMACIÓN</span><h2 id="trip-form-title">{item.id ? "Editar pesca" : "Nueva pesca"}</h2><p>Registra los datos principales de tu salida.</p></div><button className="icon-button" onClick={close} aria-label="Cerrar"><X /></button></div><div className="stepper"><i className="active" /><i /></div><form onSubmit={(e) => void submit(e)}><div className="form-grid"><Field label="Título de la pesca" value={form.title} set={(v) => update("title", v)} placeholder="Ej. Amanecer en Progreso" required wide /><label>Marina o puerto <em>*</em><select value={form.port} onChange={(e) => update("port", e.target.value)} required><option value="">Selecciona un puerto</option>{ports.filter((p) => p.active).map((p) => <option key={p.id}>{p.name}</option>)}</select></label><Field label="Fecha" type="date" value={form.fishingDate} set={(v) => update("fishingDate", v)} required /><Field label="Hora de salida" type="time" value={form.departureTime} set={(v) => update("departureTime", v)} /><Field label="Hora de regreso" type="time" value={form.returnTime} set={(v) => update("returnTime", v)} />{form.port === "Otro" && <Field label="Nombre del puerto" value={customPort} set={setCustomPort} placeholder="Escribe el puerto o marina" required wide />}<Field label="Zona aproximada" value={form.area} set={(v) => update("area", v)} placeholder="Ej. Arrecife Alacranes" wide /><Field label="Embarcación" value={form.vessel} set={(v) => update("vessel", v)} placeholder="Nombre opcional" /><Field label="Capitán" value={form.captain} set={(v) => update("captain", v)} placeholder="Nombre opcional" /><label className="wide">Notas<textarea value={form.notes} onChange={(e) => update("notes", e.target.value)} placeholder="Condiciones, acompañantes o recuerdos de la salida" rows={3} /></label><label className="wide">Estado<select value={form.status} onChange={(e) => update("status", e.target.value)}><option value="DRAFT">Borrador</option><option value="COMPLETED">Finalizada</option></select></label></div><div className="sheet-footer"><button className="button secondary" type="button" onClick={close}>Cancelar</button><button className="button secondary" type="submit" disabled={saving}>{saving ? "Guardando…" : "Guardar borrador"}</button><PrimaryButton type="button" disabled={saving} onClick={() => { const fake = { preventDefault() {} } as FormEvent; void submit(fake, true); }}>Guardar y agregar peces <ChevronDown size={17} /></PrimaryButton></div></form></div></div>; }

function CatchForm({ config, species, save, close, saving }: { config: { tripId: string; item?: Catch }; species: CatalogItem[]; save: (p: Record<string, unknown>, file: File | null) => Promise<void>; close: () => void; saving: boolean }) { const item = config.item; const initialSpecies = item?.customSpecies ? "Otro" : item?.species || ""; const [form, setForm] = useState({ species: initialSpecies, customSpeciesName: item?.customSpecies ? item.species : "", weight: item?.originalWeight?.toString() || "", weightUnit: item?.originalUnit || "kg", releaseStatus: item?.releaseStatus || "UNSPECIFIED", length: item?.lengthCm?.toString() || "", caughtAt: item?.caughtAt || "", lure: item?.lure || "", notes: item?.notes || "" }); const [search, setSearch] = useState(""); const [file, setFile] = useState<File | null>(null); const update = (key: string, value: string) => setForm({ ...form, [key]: value }); const filtered = species.filter((s) => s.active && `${s.commonName} ${s.aliases}`.toLowerCase().includes(search.toLowerCase())); return <div className="modal-layer" role="dialog" aria-modal="true" aria-labelledby="catch-form-title"><div className="sheet catch-sheet"><div className="sheet-head"><div><span className="eyebrow">PASO 2 DE 2 · CAPTURA</span><h2 id="catch-form-title">{item ? "Editar captura" : "Agregar pez"}</h2><p>Un registro por pez para estadísticas más claras.</p></div><button className="icon-button" onClick={close} aria-label="Cerrar"><X /></button></div><div className="stepper"><i className="active" /><i className="active" /></div><form onSubmit={(e) => { e.preventDefault(); void save(form, file); }}><label className="species-search"><Search /><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Buscar especie o alias (ej. curél)" /></label><div className="species-picker" role="radiogroup" aria-label="Seleccionar especie">{filtered.slice(0, 12).map((s) => <button type="button" role="radio" aria-checked={form.species === s.commonName} className={form.species === s.commonName ? "selected" : ""} key={s.id} onClick={() => update("species", s.commonName || "")}><span><Fish /></span>{s.commonName}</button>)}</div>{form.species === "Otro" && <Field label="Nombre del pez" value={form.customSpeciesName} set={(v) => update("customSpeciesName", v)} placeholder="Nombre común" required />}<div className="form-grid catch-fields"><Field label="Peso individual" type="number" value={form.weight} set={(v) => update("weight", v)} placeholder="0.00" required /><label>Unidad<select value={form.weightUnit} onChange={(e) => update("weightUnit", e.target.value)}><option value="kg">Kilogramos</option><option value="lb">Libras</option></select></label><label>Captura<select value={form.releaseStatus} onChange={(e) => update("releaseStatus", e.target.value)}><option value="UNSPECIFIED">No especificado</option><option value="KEPT">Conservado</option><option value="RELEASED">Liberado</option></select></label><Field label="Longitud (cm)" type="number" value={form.length} set={(v) => update("length", v)} placeholder="Opcional" /><Field label="Hora aproximada" type="time" value={form.caughtAt} set={(v) => update("caughtAt", v)} /><Field label="Señuelo o carnada" value={form.lure} set={(v) => update("lure", v)} placeholder="Ej. sardina" /><label className="wide">Observaciones<textarea rows={2} value={form.notes} onChange={(e) => update("notes", e.target.value)} placeholder="Detalles de la captura" /></label><label className="photo-drop wide"><Camera /><b>{file ? file.name : "Agregar fotografía"}</b><small>JPG, PNG o WebP · máximo 8 MB</small><input type="file" accept="image/jpeg,image/png,image/webp" onChange={(e) => setFile(e.target.files?.[0] || null)} /></label></div><div className="sheet-footer"><button className="button secondary" type="button" onClick={close}>Cancelar</button><PrimaryButton type="submit" disabled={saving || !form.species || !form.weight}>{saving ? "Guardando…" : item ? "Actualizar captura" : "Registrar captura"}</PrimaryButton></div></form></div></div>; }

function Field({ label, value, set, type = "text", placeholder, required = false, wide = false }: { label: string; value: string; set: (v: string) => void; type?: string; placeholder?: string; required?: boolean; wide?: boolean }) { return <label className={wide ? "wide" : ""}>{label}{required && <em> *</em>}<input type={type} value={value} onChange={(e) => set(e.target.value)} placeholder={placeholder} required={required} min={type === "number" ? "0.01" : undefined} step={type === "number" ? "0.01" : undefined} /></label>; }
function ConfirmDialog({ title, body, action, close, saving }: { title: string; body: string; action: () => void; close: () => void; saving: boolean }) { return <div className="modal-layer" role="alertdialog" aria-modal="true"><div className="confirm-box"><span className="danger-icon"><Trash2 /></span><h2>{title}</h2><p>{body}</p><div><button className="button secondary" onClick={close}>Cancelar</button><button className="button danger-fill" onClick={action} disabled={saving}>{saving ? "Eliminando…" : "Sí, eliminar"}</button></div></div></div>; }
function EmptyState({ newTrip }: { newTrip: () => void }) { return <div className="card empty-state"><div className="empty-illustration"><Waves /><span><Fish /></span><Anchor /></div><h2>Tu próxima historia empieza en el mar</h2><p>Aún no has registrado pescas. Crea tu primera salida y conserva cada captura, foto y récord.</p><PrimaryButton onClick={newTrip}><Plus size={18} />Registrar mi primera pesca</PrimaryButton></div>; }
function LoadingScreen() { return <div className="loading-screen"><Brand /><div className="loading-fish"><Fish /></div><p>Preparando tu bitácora…</p></div>; }
function ErrorScreen({ message, retry }: { message: string; retry: () => void }) { return <div className="error-screen"><span><Waves /></span><h1>Algo se movió con la marea</h1><p>{message}</p><PrimaryButton onClick={retry}>Intentar nuevamente</PrimaryButton></div>; }
function Noop() { return null; }

function initials(name: string) { return (name || "YF").split(" ").slice(0, 2).map((x) => x[0]).join("").toUpperCase(); }
function tripCatches(id: string, catches: Catch[]) { return catches.filter((c) => c.tripId === id); }
function tripWeight(id: string, catches: Catch[]) { return tripCatches(id, catches).reduce((sum, c) => sum + Number(c.weightKg), 0); }
function groupSpecies(catches: Catch[]) { return Object.entries(catches.reduce<Record<string, number>>((a, c) => ({ ...a, [c.species]: (a[c.species] || 0) + 1 }), {})).map(([name, count]) => ({ name, count })).sort((a, b) => b.count - a.count); }
function filteredData(data: AppData, period: string) { const today = new Date(`${mxDate()}T12:00:00`); const start = new Date(today); if (period === "week") start.setDate(start.getDate() - 7); else if (period === "month") start.setMonth(start.getMonth() - 1); else if (period === "year") start.setFullYear(start.getFullYear() - 1); else return { trips: data.trips, catches: data.catches }; const trips = data.trips.filter((t) => new Date(`${t.fishingDate}T12:00:00`) >= start); const ids = new Set(trips.map((t) => t.id)); return { trips, catches: data.catches.filter((c) => ids.has(c.tripId)) }; }
function calculate(data: AppData, period: string) { const { trips, catches } = filteredData(data, period); const groups = groupSpecies(catches); return { trips, catches, totalWeight: catches.reduce((s, c) => s + Number(c.weightKg), 0), heaviest: Math.max(...catches.map((c) => Number(c.weightKg)), 0), released: catches.filter((c) => c.releaseStatus === "RELEASED").length, topSpecies: groups[0]?.name || "" }; }
function monthCounts(data: AppData) { const formatter = new Intl.DateTimeFormat("es-MX", { month: "short" }); const months = Array.from({ length: 6 }, (_, index) => { const d = new Date(); d.setMonth(d.getMonth() - (5 - index)); return { key: `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}`, label: formatter.format(d).replace(".", ""), value: 0 }; }); const byTrip = new Map(data.trips.map((t) => [t.id, t.fishingDate.slice(0, 7)])); data.catches.forEach((c) => { const m = months.find((x) => x.key === byTrip.get(c.tripId)); if (m) m.value++; }); return months; }

void Noop;
