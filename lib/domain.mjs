export function normalizeName(value) { return value.normalize("NFD").replace(/[\u0300-\u036f]/g, "").trim().toLowerCase().replace(/\s+/g, " "); }
export function weightToKg(value, unit) { if (!Number.isFinite(value) || value <= 0) throw new Error("invalid_weight"); return unit === "lb" ? value * 0.45359237 : value; }
export function weightFromKg(value, unit) { return unit === "lb" ? value * 2.20462262 : value; }
export function calculateStats(catches) { const totalWeight = catches.reduce((sum, item) => sum + Number(item.weightKg), 0); const counts = catches.reduce((all, item) => ({ ...all, [item.species]: (all[item.species] || 0) + 1 }), {}); const topSpecies = Object.entries(counts).sort((a, b) => b[1] - a[1])[0]?.[0] || null; return { total: catches.length, totalWeight, averageWeight: catches.length ? totalWeight / catches.length : 0, heaviest: Math.max(0, ...catches.map((item) => Number(item.weightKg))), released: catches.filter((item) => item.releaseStatus === "RELEASED").length, distinctSpecies: Object.keys(counts).length, topSpecies }; }
export function achievements({ trips, catches, photos }) { return { firstTrip: trips.length >= 1, tenTrips: trips.length >= 10, firstPhoto: photos.length >= 1, fiveSpecies: new Set(catches.map((item) => item.species)).size >= 5 }; }
